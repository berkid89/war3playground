@model war3playground.Models.StandardPage
@{
    ViewBag.Title = Model.Title;
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-sm-12">
            <h1>@Model.Title</h1>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-sm-12">
            <p>A kötetlen beszélgetések helye.</p>
        </div>
    </div>
    <partial name="/Views/Shared/Partial/_Disqus.cshtml" model="@Model.Id" />
</div>

